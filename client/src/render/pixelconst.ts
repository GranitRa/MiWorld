// Texel-density convention for the whole HD-2D look. Sprites are deliberately denser than
// the environment (authentic HD-2D): a colonist's pixels are finer than the ground's.
export const TERRAIN_PX_PER_M = 4;
export const BUILDING_PX_PER_M = 8;
export const COLONIST_PX_TALL = 28; // for ~1.7 m
