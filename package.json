{
  "name": "miworld",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A persistent, self-evolving Mars colony you only watch.",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server",
    "build": "npm run build --workspace client && npm run build --workspace server",
    "start": "node server/dist/index.js",
    "test": "npm run test --workspace shared --workspace server --workspace client --if-present",
    "typecheck": "npm run typecheck --workspace shared --workspace server --workspace client --if-present"
  },
  "allowScripts": {
    "esbuild@0.24.2": true,
    "esbuild@0.28.1": true,
    "esbuild@0.21.5": true
  }
}
